{
  "name": "@qdex/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Local-only Hardhat harness for QDEX non-custodial settlement contracts.",
  "type": "commonjs",
  "scripts": {
    "check": "node scripts/guard-local-only-hardhat-config.mjs",
    "test": "pnpm run check",
    "test:local": "hardhat test --network hardhat"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.3",
    "ethers": "^5.8.0",
    "hardhat": "^2.19.5"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "quais": "^1.0.0-alpha.54"
  }
}
